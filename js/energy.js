import {showModal, hideModal } from './common.js';
import { ajaxRequest } from './ajax.js';

console.log("energy.js loaded");

// Classic id places

// -- Form add central

// -- Form edit central

// -- Graph production 

// -- Graph conso


// Basic function
// -- Fill graph
function fillGraph(graph, data, parameter){
    /*
    Take a graph and put the data inside using the parameter to make the proportions
    graph       = element of the document 
    data        = data from the database
    parameter   = a string to use in data['parameter]
    console.log(graph, data);
    */
}

// -- 

// -- Create forms
function createForm(data){
    /*
    Take the data from the sql and make the form of the production 
    data        = contain all data in need
    data[]      = machine used in the 
    */
}

// -- Modals 



